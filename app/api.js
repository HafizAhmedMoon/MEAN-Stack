var express = require('express');
var router = express.Router();

    //Example
     /*router.get('/user', function(req, res) {
     req.db.user.find(function(err,data){
            if(err) return console.error(err);
            console.log('get');
            res.json(data);
            res.end();
        });
    });*/


module.exports = router;